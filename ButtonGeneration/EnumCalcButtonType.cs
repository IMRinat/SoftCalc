namespace ButtonGeneration
{
    public enum EnumCalcButtonType
    {
        Empty, // пустое место
        NextRow, // переход на следующую строку
        Evalute, //расчет выражения
        Delete, // стереть значение
        Clear, // очистить строку
        Digit, // цифра
        Unar, //  операция с одним аргументом
        Binar, //операция  с двумя аргументами
        Bracket //скобки
    }
}
